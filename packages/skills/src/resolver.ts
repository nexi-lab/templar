/**
 * Re-export the SkillResolver interface.
 *
 * This file exists as a single import point for the resolver interface.
 * Concrete implementations live in separate files (local-resolver.ts, etc.).
 */

export type { SkillResolver } from "./types.js";
