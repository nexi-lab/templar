/** 100KB — generous for chat/messaging, prevents ReDoS on huge inputs */
export const DEFAULT_MAX_INPUT_LENGTH = 102_400;

/** Maximum length of matched content stored in violations */
export const DEFAULT_MAX_MATCH_LENGTH = 200;
