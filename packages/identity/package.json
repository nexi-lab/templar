{
  "name": "@templar/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Per-channel agent identity resolution for Templar",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@templar/core": "workspace:*"
  }
}
